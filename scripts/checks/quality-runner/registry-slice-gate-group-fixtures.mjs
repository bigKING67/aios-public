import {
  QUALITY_RUNNER_AFFECTED_GATES,
  QUALITY_RUNNER_BENCHMARK_GATES,
  QUALITY_RUNNER_CACHE_EXECUTION_GATES,
  QUALITY_RUNNER_CACHE_GATES,
  QUALITY_RUNNER_CACHE_STATS_GATES,
  QUALITY_RUNNER_CORE_GATES,
  QUALITY_RUNNER_ENTRYPOINT_GATES,
  QUALITY_RUNNER_HOOK_GATES,
  QUALITY_RUNNER_MANIFEST_GATES,
  QUALITY_RUNNER_PREFLIGHT_CACHE_GATES,
  QUALITY_RUNNER_PREPUSH_GATES,
  QUALITY_RUNNER_REGISTRY_GATES,
  QUALITY_RUNNER_SCHEDULER_GATES,
} from '../../lib/quality/quality-affected-gates.mjs';
import {
  QUALITY_RUNNER_AFFECTED_SLICE_GATES,
  QUALITY_RUNNER_BENCHMARK_SLICE_GATES,
  QUALITY_RUNNER_CACHE_EXECUTION_SLICE_GATES,
  QUALITY_RUNNER_CACHE_SLICE_GATES,
  QUALITY_RUNNER_CACHE_STATS_SLICE_GATES,
  QUALITY_RUNNER_CORE_SLICE_GATES,
  QUALITY_RUNNER_ENTRYPOINT_SLICE_GATES,
  QUALITY_RUNNER_HOOK_SLICE_GATES,
  QUALITY_RUNNER_MANIFEST_SLICE_GATES,
  QUALITY_RUNNER_PREFLIGHT_CACHE_SLICE_GATES,
  QUALITY_RUNNER_PREPUSH_SLICE_GATES,
  QUALITY_RUNNER_REGISTRY_SLICE_GATES,
  QUALITY_RUNNER_SCHEDULER_SLICE_GATES,
} from '../../lib/quality/quality-runner-slices.mjs';

export function assertRegistrySliceGateGroupMetadata({
  assertDeepEqual,
}) {
  for (const [actual, expected, label] of [
    [QUALITY_RUNNER_REGISTRY_GATES, QUALITY_RUNNER_REGISTRY_SLICE_GATES, 'registry'],
    [QUALITY_RUNNER_AFFECTED_GATES, QUALITY_RUNNER_AFFECTED_SLICE_GATES, 'affected'],
    [QUALITY_RUNNER_BENCHMARK_GATES, QUALITY_RUNNER_BENCHMARK_SLICE_GATES, 'benchmark'],
    [QUALITY_RUNNER_CACHE_GATES, QUALITY_RUNNER_CACHE_SLICE_GATES, 'cache'],
    [QUALITY_RUNNER_CACHE_STATS_GATES, QUALITY_RUNNER_CACHE_STATS_SLICE_GATES, 'cache stats'],
    [QUALITY_RUNNER_CACHE_EXECUTION_GATES, QUALITY_RUNNER_CACHE_EXECUTION_SLICE_GATES, 'cache execution'],
    [QUALITY_RUNNER_ENTRYPOINT_GATES, QUALITY_RUNNER_ENTRYPOINT_SLICE_GATES, 'entrypoint'],
    [QUALITY_RUNNER_MANIFEST_GATES, QUALITY_RUNNER_MANIFEST_SLICE_GATES, 'manifest'],
    [QUALITY_RUNNER_SCHEDULER_GATES, QUALITY_RUNNER_SCHEDULER_SLICE_GATES, 'scheduler'],
    [QUALITY_RUNNER_HOOK_GATES, QUALITY_RUNNER_HOOK_SLICE_GATES, 'hook'],
    [QUALITY_RUNNER_PREPUSH_GATES, QUALITY_RUNNER_PREPUSH_SLICE_GATES, 'prepush'],
    [QUALITY_RUNNER_PREFLIGHT_CACHE_GATES, QUALITY_RUNNER_PREFLIGHT_CACHE_SLICE_GATES, 'preflight cache'],
    [QUALITY_RUNNER_CORE_GATES, QUALITY_RUNNER_CORE_SLICE_GATES, 'core'],
  ]) {
    assertDeepEqual(
      actual,
      expected,
      `affected ${label} quality-runner gate group should be derived from slice metadata`,
    );
  }
}
